import Foundation

// RunWriteLock / RunWriteLockRegistry live in ExecutionLane.swift (PO-S03).
// One per-root lane system, one key — this file remains only so historical
// paths and docs that name RunWriteLock.swift still resolve. Do not reintroduce
// a second registry here.
