# composer — layout-watcher verdict (Auto draws from tiers)

Fixtures: home-with-threads (Auto / single model) · compose-team (team selected)

## VERDICT: PASS

P1 — broken (blocks): none
P2 — advisory:
- compose-team is the standalone ComposeSpecimen — no sidebar/thread history above the
  bar (by design).

Capture A target chip reads "Opus 4.8 · Med" (the tier-resolved Auto model) — fully
legible, clear of the send/image buttons, project line above the box, no helper line.
Capture B reads team glyph + "Design Core · 7 workers". Both clean.
