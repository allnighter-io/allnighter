# composer — layout-watcher verdict

Fixtures: home-with-threads (single-model) · compose-team (team selected)

## VERDICT: PASS

P1 — broken (blocks): none
P2 — advisory:
- compose-team: standalone specimen has no sidebar and the box sits low — both by
  design (Spacer + composer at the bottom of the proof canvas). Not clipped.

Both renders structurally intact: the read-only "Project · branch" line floats above
the box; inside, the single target chip reads "Composer 2.5 · Med" (model · effort) in
A and "Design Core · 7 workers" with a team glyph in B — never a fake model · effort.
No helper line. Chip text fully legible, clear of the send/image buttons.
