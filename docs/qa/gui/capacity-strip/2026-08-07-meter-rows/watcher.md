# Layout-watcher verdict — capacity-strip · meter-rows

Date: 2026-08-07
Fixtures: `capacity-strip`, `capacity-strip-calm`
Renders: sealed native captures in this packet
Watcher: layout-watcher subagent (separate — did not write the change)

## VERDICT: PASS

**P1 — broken (blocks): none**

**P2 — advisory:**
- Partial "—" glyph at the table bottom edge (likely Qwen row clipped by the
  composer). Seat count and meter flattening are otherwise clean.

## Summary

One measured capacity = one row. Weekly bars share one left edge; Last 5h
trails as a column; Antigravity splits into adjacent `· Gemini` /
`· Claude/GPT` siblings; OpenCode is labeled OpenCode (Go is plan tier only).
