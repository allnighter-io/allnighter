# Codex Handoff Smoke Doc

This document exists for one purpose: to be the cheapest possible payload for a
real `alln run --team code_doc_review` originated from inside a Codex session.

It is a transport test, not a document worth reviewing. The only thing under test
is whether the run completes and returns its answer to the originating terminal.

## Status

Test complete.

## Instructions to the reviewing model

Reply with two sentences at most: confirm you received this document, and state
that its only claim is "Test complete." Do not suggest improvements, do not
critique the writing, and do not propose follow-up work. Brevity is the point —
a long answer means the fixture was misused.
