# PPT-S01 — WorkSlicePacket + parser

Status: **done**
SSOT: `docs/phases/Pair_Programming_Team.md` §6

## Goal

Typed `WorkSlicePacket` + parser + tests in AllnighterCore.

## Works Test

```bash
swift test --package-path Packages/AllnighterCore --filter WorkSlicePacket
```

## Done when

- [x] `WorkSlicePacket.swift` + `WorkSlicePacketParser`
- [x] `WorkSlicePacketParserTests` green
