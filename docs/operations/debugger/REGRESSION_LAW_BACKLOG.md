# Regression Law Backlog

DEBUGLOG entries without a wall-reachable regression law are tracked here until
each pattern has a gate or test. Expired rows should fail the green wall once
meta-gates exist.

No active backlog rows.
