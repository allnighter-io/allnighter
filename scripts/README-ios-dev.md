# iOS dev scripts

**Read first:** [docs/operations/ios-testing-loop.md](../docs/operations/ios-testing-loop.md)

Quick default (preview, no Mac):

```bash
cd /Users/mike/Documents/GitHub/Allnighter-iOS && allios
```

Fast relaunch:

```bash
cd /Users/mike/Documents/GitHub/Allnighter-iOS && allios launch
```

`allios` is wired in `~/.zshrc` to `scripts/ios_dev.sh`. See the testing-loop doc
for live relay, tests, and agent rules.
