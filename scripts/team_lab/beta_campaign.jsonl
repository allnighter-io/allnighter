# Allnighter beta team campaign — the 6 teams we intend to ship.
# One JSON job per line (lines starting with # are ignored). The supervisor runs
# these serialized, non-stop, with retry/backoff/heartbeat, and skips `done` jobs
# on restart. `blocked: true` jobs are reported, not silently skipped.
#
# STATUS LEGEND (see docs): READY = roster + suite exist, runnable now.
#                           BLOCKED = needs a roster overlay and/or a necessity suite first.
#
# --- CODE craft (rosters + suites exist; runnable once quota is authorized) ---
{"id": "code_bug_hunt_lite__validate", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "docs/team-lab/champions/bug_hunt_repo_regressions_v1/code_bug_hunt_lite.json", "bug_hunt_repo_regressions_v1", "beta_validate"], "retries": 2, "timeoutSec": 10800, "capacityBackoffSec": 2400, "note": "Bug Hunt Lite — 4-seat baseline Code team. Closest to rock-solid; validate clean pass across the 3 known-regression cases."}
{"id": "code_bug_hunt__validate", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "docs/team-lab/champions/bug_hunt_repo_regressions_v1/code_bug_hunt.json", "bug_hunt_repo_regressions_v1", "beta_validate"], "retries": 2, "timeoutSec": 10800, "capacityBackoffSec": 2400, "note": "Bug Hunt — fuller Code roster for harder T3 seam cases."}
# NOTE: the teams below already EXIST as built-ins in BuiltInTeams.swift. The real
# blocker is the validation SUITE per craft — all 3 existing suites are lane:code.
# Once a craft suite exists, swap blocked:true for a runnable validate_team.sh job
# using team:<built-in id>.
{"id": "code_build__needs_suite", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "team:code_core", "PLAN_SUITE_TODO", "beta_validate"], "blocked": true, "note": "Code Build = built-in team code_core (lane:code, output .plan). Blocked only on a build/plan necessity suite (spec_upgrade_mcp_lab_v1 has 1 case; want a real multi-case plan suite)."}
{"id": "design_board__needs_suite", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "team:design_core", "DESIGN_SUITE_TODO", "beta_validate"], "blocked": true, "note": "Design = built-in team design_core (lane:design, output .designBoard). Blocked only on a design necessity suite."}
{"id": "copy__needs_suite", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "team:copy_core", "COPY_SUITE_TODO", "beta_validate"], "blocked": true, "note": "Copy = built-in team copy_core (lane:copy, output .copyBoard). Blocked only on a copy necessity suite."}
{"id": "signal__needs_suite", "cmd": ["bash", "scripts/team_lab/validate_team.sh", "team:signal_what_to_build_next", "SIGNAL_SUITE_TODO", "beta_validate"], "blocked": true, "note": "Signal = built-in team signal_what_to_build_next (lane:signal, output .insight). Blocked only on a signal necessity suite."}
